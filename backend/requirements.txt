# Aql Zone — backend bog'liqliklari
#
#   pip install -r requirements.txt
#
# Ataylab qisqa: CORS, statik fayl va throttling uchun tashqi paket
# ishlatilmaydi — hammasi Django/DRF ning o'zida bor.
Django>=5.2,<7
djangorestframework>=3.15

# Ishlab chiqarish serveri. `runserver` faqat ishlab chiqish uchun —
# u bitta oqimda ishlaydi va Django hujjatining o'zi uni ochiq
# internetda ishlatmaslikni aytadi.
gunicorn>=23.0

# Rasm QAYTA YOZISH uchun (`core/rasm.py`). Faqat "rasmni ko'rsatish"
# uchun emas — kelgan fayl haqiqatan rasmmi degan yagona ishonchli
# tekshiruv shu: uni ochib, qaytadan kodlash. Ustiga EXIF ham
# tushib qoladi, ya'ni bolaning suratidagi GPS koordinatasi saytga
# chiqib ketmaydi.
Pillow>=11.0
